const Numbers = (numbers:number) => {
           return(
               <div className= "number">
                  <strong>{numbers}</strong>
               </div>
           );
}

export default  Numbers